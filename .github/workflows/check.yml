name: Check

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    name: ${{ matrix.build_type }} Build (${{ matrix.compiler.name }})
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel
    strategy:
      fail-fast: false
      matrix:
        build_type: [Release]
        compiler:
          - name: GCC
            cc: gcc
            cxx: g++
          - name: Clang
            cc: clang
            cxx: clang++

    steps:
      - name: Cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: ${{ runner.os }}-arch-pacman-${{ hashFiles('.github/workflows/check.yml') }}
          restore-keys: |
            ${{ runner.os }}-arch-pacman-

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /github/home/.cache/ccache
          key: ${{ runner.os }}-ccache-${{ matrix.compiler.name }}-${{ matrix.build_type }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-${{ matrix.compiler.name }}-${{ matrix.build_type }}-
            ${{ runner.os }}-ccache-${{ matrix.compiler.name }}-
            ${{ runner.os }}-ccache-

      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --needed --noconfirm \
            ccache \
            clang \
            cmake \
            extra-cmake-modules \
            fcitx5 \
            gcc \
            git \
            hicolor-icon-theme \
            libchewing \
            pkgconf

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run release checks
        run: >
          ARI_IME_CHECK_MODE=release
          ARI_IME_BUILD_TYPE=${{ matrix.build_type }}
          ARI_IME_BUILD_DIR=build-${{ matrix.compiler.name }}-${{ matrix.build_type }}
          ARI_IME_CC=${{ matrix.compiler.cc }}
          ARI_IME_CXX=${{ matrix.compiler.cxx }}
          ARI_IME_CXX_COMPILER_LAUNCHER=ccache
          scripts/check.sh

  sanitize:
    name: Sanitizers
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel

    steps:
      - name: Cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: ${{ runner.os }}-arch-pacman-${{ hashFiles('.github/workflows/check.yml') }}
          restore-keys: |
            ${{ runner.os }}-arch-pacman-

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /github/home/.cache/ccache
          key: ${{ runner.os }}-ccache-sanitize-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-sanitize-
            ${{ runner.os }}-ccache-

      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --needed --noconfirm \
            ccache \
            cmake \
            extra-cmake-modules \
            fcitx5 \
            gcc \
            git \
            hicolor-icon-theme \
            libchewing \
            pkgconf

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run sanitizer checks
        run: ARI_IME_CHECK_MODE=sanitize ARI_IME_CXX_COMPILER_LAUNCHER=ccache scripts/check.sh

  fuzz:
    name: Bounded Fuzz
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel

    steps:
      - name: Cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: ${{ runner.os }}-arch-pacman-${{ hashFiles('.github/workflows/check.yml') }}
          restore-keys: |
            ${{ runner.os }}-arch-pacman-

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /github/home/.cache/ccache
          key: ${{ runner.os }}-ccache-fuzz-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-fuzz-
            ${{ runner.os }}-ccache-

      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --needed --noconfirm \
            ccache \
            clang \
            cmake \
            extra-cmake-modules \
            fcitx5 \
            gcc \
            git \
            hicolor-icon-theme \
            libchewing \
            pkgconf

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run bounded fuzz checks
        env:
          ARI_IME_CHECK_MODE: fuzz
          ARI_IME_CXX_COMPILER_LAUNCHER: ccache
          ARI_IME_FUZZ_RUNS: 256
          ARI_IME_FUZZ_ARTIFACT_DIR: fuzz-artifacts
        run: scripts/check.sh

      - name: Upload fuzz crash artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: bounded-fuzz-artifacts-${{ github.run_id }}
          path: fuzz-artifacts
          if-no-files-found: ignore

  package:
    name: Package Simulation
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel

    steps:
      - name: Cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: ${{ runner.os }}-arch-pacman-${{ hashFiles('.github/workflows/check.yml') }}
          restore-keys: |
            ${{ runner.os }}-arch-pacman-

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /github/home/.cache/ccache
          key: ${{ runner.os }}-ccache-package-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-package-
            ${{ runner.os }}-ccache-

      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --needed --noconfirm \
            ccache \
            cmake \
            extra-cmake-modules \
            fcitx5 \
            gcc \
            git \
            hicolor-icon-theme \
            libchewing \
            pkgconf

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run package simulation
        run: ARI_IME_CHECK_MODE=package CMAKE_CXX_COMPILER_LAUNCHER=ccache scripts/check.sh
